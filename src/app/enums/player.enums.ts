export enum PlaybackStatus {
    PLAYING = 'playing',
    PAUSED = 'paused',
}

export enum LoopStatus {
    ENABLED = 'enabled',
    DISABLED = 'disabled',
}

export enum ShuffleStatus {
    ENABLED = 'enabled',
    DISABLED = 'disabled',
}
